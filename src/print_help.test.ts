/*
  test prints only usage and inbuilt options/commands with stub command
  test prints (sorted) custom subcommands as well if included
  test prints (sorted) custom options as well if included
  test prints description if included
  test prints examples if included
*/
import { terminal } from "./Terminal"

const print_header = (str: string) => {
  const h = (terminal.indent / 2) + 1;
  return `${'#'.repeat(h)} ${str}`;
}
export function generate_docs (exe: string, cmd: Command): string {


  terminal.print_line(`${}`)
  command

  description
  
  examples


}